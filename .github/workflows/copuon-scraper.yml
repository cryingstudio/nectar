name: Daily Coupon Scraper

on:
  schedule:
    # Run at midnight UTC every day
    - cron: "0 0 * * *"
  workflow_dispatch: # Allows manual triggering for testing

jobs:
  scrape-coupons:
    runs-on: ubuntu-latest
    timeout-minutes: 60 # Increased timeout to allow for modal processing

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: useblacksmith/setup-node@v5
        with:
          node-version: "18"

      - name: Install dependencies
        working-directory: ./scraper
        run: npm install

      - name: Run coupon scraper
        env:
          SUPABASE_URL: ${{ env.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ env.SUPABASE_ANON_KEY }}
        run: node scraper/scraper.js
          echo "This is the ${{ env.SUPABASE_URL }} environment variable"
          echo "This is the ${{ env.SUPABASE_ANON_KEY }} environment variable"
